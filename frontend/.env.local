# Base URL of the TWS E-Commerce API, including the version prefix.
# Matches API_PORT in the repository root .env (default 58000).
NEXT_PUBLIC_API_BASE_URL=http://localhost:58000/api/v1

# Display formatting only -- never sent to the API.
NEXT_PUBLIC_CURRENCY=INR
NEXT_PUBLIC_LOCALE=en-IN
